# Results

The published proof-of-concept values are stored in `published_summary.csv`.

Running `python experiments/run_proof_of_concept.py` creates `results/generated/synthetic_proof_of_concept.json`.

The synthetic script is a smoke test and teaching example, not a byte-for-byte reproduction of the archived Kaggle run.
